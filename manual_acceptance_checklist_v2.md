# 人工验收清单 v2（2026-07-06）

> **本清单取代 v1**（v1 基于旧交互：场景三选是主线、5条消息弹窗、"记下来"按钮、解锁/锁回去，与当前代码对不上，已于 2026-07-06 删除，历史版本在 git 里）。
> 本清单按当前实际实现（handoff v7/v8 设计：日推卡片、"说完了"机制、图鉴3激活+放下、回顾生成）逐路径编写。
> 每条通过就打 `[x]`；不通过记下现象，回头一起处理。H5 与 mp-weixin 各跑一遍（先 H5 熟悉流程，再小程序复核）。

## 0. 准备工作

1. `.env` 检查（项目根目录）：
   - `VITE_QWEN_MODEL`、`VITE_DEEPSEEK_MODEL` —— 对话与摘要必需
   - `VITE_API_PROXY_URL` —— 两种形态二选一：
     - 本地代理：不填（默认 `http://localhost:5555`），另开终端跑 `npm run proxy`
     - Supabase 线上代理：填 `https://<project>.supabase.co/functions/v1`，并配 `VITE_SUPABASE_ANON_KEY`
   - `VITE_WEATHER_API_KEY` —— 可选，不配则城市/天气走"未知"降级（这本身也是一条验收项，见 §7）
2. 启动：
   - H5：`npm run dev:h5`，浏览器打开输出的地址
   - mp-weixin：`npm run dev:mp-weixin`，微信开发者工具导入 `dist/dev/mp-weixin`
3. 清一次存储从全新状态开始：H5 用 devtools → Application → Local Storage 清空；小程序用调试器 → Storage 清空

---

## 1. 首次启动：呼吸 → 基本信息 → 日推卡片

- [ ] 1.1 打开应用见呼吸引导，上方常驻语境文案（"这里每天有一些小事……"），有跳过按钮
- [ ] 1.2 全新状态下呼吸结束后，先弹**基本信息设置**（玩家ID / 出生日期选择器 / 三组场景多选），保存后自动弹出日推卡片
- [ ] 1.3 日推卡片头部依次显示：`尊敬的地球online玩家：<ID>`、累计生存时长（填了出生日期才有）、今日编号、登陆城市、区域环境情况
- [ ] 1.4 固定提醒三句话完整显示（"请重视自己的感受……人生是一场体验"）
- [ ] 1.5 候选任务列表每条含 标题 + hook + 领取按钮
- [ ] 1.6 "换一批"最多点 3 次；第 3 次后按钮消失，显示兜底文案（"换了好几批了，没找到今天想做的也没关系……"）
- [ ] 1.7 基本信息不完整时（比如没填生日），卡片底部出现"去完善你的信息 →"，点击进入基本信息页，返回后卡片还在
- [ ] 1.8 关闭卡片后，主页"今日任务候选"常驻入口可以重新打开卡片（复用当天数据，不重新请求天气）
- [ ] 1.9 当天再次刷新/重启应用，卡片**不再自动弹出**；清掉存储里的 `DAILY_CARD_SHOWN_DATE`（或等次日）后再次打开会弹

## 2. 每日任务闭环（"当下" tab）

- [ ] 2.1 卡片内点"领取"，按钮变"已领取"不可再点；关闭卡片后任务出现在"我的日常任务"区块
- [ ] 2.2 点已领取任务 → 详情卡（标题/时长/做法）+ 三个操作：做完啦 / ← 返回 / 不想做了，移除
- [ ] 2.3 "移除"：任务从"我的日常任务"消失，且**不**出现在"今日已完成"
- [ ] 2.4 "做完啦" → 弹出聊聊邀请文案；点"跳过"回到主页
- [ ] 2.5 完成的任务出现在"今日已完成"区块（灰色 + 聊聊按钮），点"聊聊"可直接进对话
- [ ] 2.6 次日打开：日推卡片 greeting 后显示昨日完成区块（"做过的就算数了，不必留着它们证明什么。"），每条有"聊聊"，底部"全部清掉"点击后区块消失
- [ ] 2.7 已领取但未完成的任务跨天仍保留在"我的日常任务"

## 3. 现在就来一件（即时任务，已取代旧主线推送流）

> remove-pushflow 变更已实施：场景三选删除，原推送层 38 条重标签并入每日任务池（共 77 条），日推卡片候选也来自这个大池（原 push_xxx 条目会带新写的 hook 出现）。

- [ ] 3.1 呼吸后主页顶部显示「让我们做点什么有意思的小事」+「希望你好好生活，别太焦虑」，下方为"现在就来一件"（深色主按钮）与"今日任务候选"入口；**页面上不存在任何场景选择残留**
- [ ] 3.2 点"现在就来一件"→ 直接出一张任务卡（标题/时长/做法），无场景选择、无候选列表；任务与你的场景偏好匹配（或为通用条目）
- [ ] 3.3 "换一个"可连续换 3 次（每次内容变化）；第 4 次点击不再更换，出现小字"如果没有想做的可以深呼吸，喝点水，发发呆"，按钮变淡
- [ ] 3.4 "做完啦"→ 聊聊邀请（聊聊/跳过）；完成的任务直接进"今日已完成"区块（**不经过**"我的日常任务"）；聊聊后"说完了"，Storage 里 completionSummaries **不**新增（推送层语义）
- [ ] 3.5 "← 返回"离开任务卡 → 主页无任何新增记录（不进列表、不进已完成）
- [ ] 3.6 空态：候选全部做完/领完时点按钮 → 显示"今天的都做过了，歇一歇也很好。"，不报错

## 4. 对话与"说完了"（各层通用）

- [ ] 4.1 进入对话即有开场白（"你刚才「××」，这件事给你带来了什么感受吗？……"），不调 API
- [ ] 4.2 发文字消息：H5 端 AI 回复逐字流式出现；mp-weixin 端一次性出现（已知平台差异，不算 bug）
- [ ] 4.3 📷 图片：选图有预览，发送后图文一起显示，AI 回应中提到了图片里的内容
- [ ] 4.4 点"说完了" → 出现收尾文案"这次说的，都在了"，短暂后关闭对话
- [ ] 4.5 推送层/每日任务对话"说完了"后：存储里**不**新增 `COMPLETION_SUMMARIES` 记录（推送层不生成摘要）
- [ ] 4.6 图鉴层对话"说完了"后：存储里出现对应摘要记录（≤100字，内容是用户说过的具体细节，无"开心/平静"类概括词）
- [ ] 4.7 摘要 API 不可用时（断网或停代理再测一条）："说完了"仍能正常关闭，无报错弹窗（归档失败静默处理）
- [ ] 4.8 对话出错（如代理未启动）时显示错误文案，不白屏不卡死

## 5. 图鉴系统（"丰荣探索" tab）

- [ ] 5.1 网格显示 11 本图鉴（感知类 5 本：颜色/角落/想法/连接/物件；事件类 6 本：城市探索/独处/自然接触/时间实验/饮食探索/人文空间），带类型标签，初始全部"未激活"，未激活卡片不显示简介
- [ ] 5.2 点卡片弹出底部弹层（名称 + 简介）："开始探索" / "算了"
- [ ] 5.3 "开始探索"后卡片变"进行中"；再点卡片可"进入图鉴"或"放下"
- [ ] 5.4 "放下"：卡片回到"未激活"；重新激活后，之前完成的条目状态仍保留
- [ ] 5.5 激活 3 本后点第 4 本未激活的：弹层显示拦截提示（"进行中的图鉴已有3个，先放下一个再来"），只有"知道了"可点；放下任意一本后即可正常激活第 4 本
- [ ] 5.6 进入图鉴 → 条目列表；点条目 → 做完啦 → 聊聊/跳过流程与主线一致
- [ ] 5.7 聊过（或已归档）的条目锁定，列表里不再显示"聊聊"入口
- [ ] 5.8 图鉴内点了"聊聊"但一句话没说就返回：条目**不**锁定，可再进入补聊（续用原对话）
- [ ] 5.9 做完最后一条 → 卡片**立即**显示"✦ 已点亮 回顾 →"（v8.2：完成瞬间零后台调用，入口不等待生成）
- [ ] 5.10 首次点开回顾：先显示邀请式加载文案（"一起回顾你为生活带来的新内容吧"），数秒后出现连续叙事（无小标题、无数据列表、结尾无格言式升华）；**最后一条目聊过的内容应出现在叙事中**（v8.2 核心验收点）；断网点开→温婉失败提示，恢复网络退出重进→正常生成
- [ ] 5.11 回顾语气抽查：只呼应用户说过的内容，**无**"你坚持完成了/做得很好/进步了"类评价
- [ ] 5.12 兜底路径：整本图鉴全部跳过聊聊做完 → 回顾使用固定陪伴模板（「有一段时间，你在《××》这件事上，认真地走了一段路……」），不编造细节
- [ ] 5.13 首次点开回顾后，之前"点了聊聊没说话"的空对话被静默被动归档，对应条目锁定（v8.2：被动归档发生在首次查看回顾时，不再是完成瞬间）
- [ ] 5.14 回顾总数 >1 时，网格页右上角出现"回顾"入口 → 全部回顾列表可查看

## 6. 设置角标

- [ ] 6.1 ⚙ 弹层三项，顺序：基本信息 / 主动提醒（去开启提醒） / 隐私政策
- [ ] 6.2 基本信息：已有值正确回填，修改保存后生效（次日日推候选与新场景偏好匹配）
- [ ] 6.3 主动提醒：mp-weixin 未配置模板 ID → "提醒功能还没配置好"；H5 → "只在小程序内可用"
- [ ] 6.4 隐私政策：显示占位文本，可返回

## 7. 降级路径

- [ ] 7.1 拒绝定位授权 / 未配天气 key：日推卡片正常展示，城市显示"未知"
- [ ] 7.2 H5 端（location 直接返回 null）：卡片正常，城市"未知"
- [ ] 7.3 断网状态下打开应用：呼吸引导、日推卡片（本地候选）、领取/完成流程均可用，仅对话和天气不可用

## 8. 指标体系上线项（add-metrics-system，全部为上线前置）

1. Supabase 服务端（一次性）：
   - [ ] 8.1 在 Supabase Dashboard → SQL Editor 整段执行 `supabase/events_table.sql`（建 events 表 + 只写 RLS）
   - [ ] 8.2 RLS 实测：用 anon key 向 `/rest/v1/events` POST 一行成功（201）；同 key GET/SELECT 被拒（401/403 或空策略拒绝）
2. 环境变量：
   - [ ] 8.3 生产构建配置 `VITE_SUPABASE_URL=https://<project>.supabase.co`（与 `VITE_API_PROXY_URL` 同项目；不配则上报整体静默禁用，功能不受影响）
3. 微信后台（只有账号本人能操作）：
   - [ ] 8.4 隐私保护指引新增收集项："使用记录"（对应匿名行为事件上报）
   - [ ] 8.5 确认 request 合法域名已含 `https://<project>.supabase.co`（LLM 代理已在用同域名，一般无需新增）
4. 端到端验证（生产模式构建，DEV 构建不上报是预期行为）：
   - [ ] 8.6 打开小程序 → 完成一个任务 → 点开一个回顾，Supabase 表里依次出现 session_start / task_completed / review_opened，载荷仅含 anon_id/event/content_type/content_id/collection_id/client_ts，无任何个人信息
   - [ ] 8.7 （非阻塞项，验不过不挡上线——失败模式安全：丢指标不丢功能）断网完成一个任务 → 联网后**打开两次**（flush 只在 onShow 试一次，刚恢复网络的首次可能网络栈未就绪），事件补发落表，且 client_ts 是断网时的实际完成时间（非补发时间）
   - [ ] 8.8 正式发布前，SQL Editor 执行 `truncate events;` 清掉全部自测数据，让正式指标从零开始（RLS 实测的 `rls-test` 行也一并清掉）

---

## 附录：已知设计-实现落差（不阻塞验收，验收后一起决策）

| # | 落差 | 出处 | 待决策 |
|---|---|---|---|
| 1 | PushFlow 仍是三分类场景选择，v7 已废弃该分类 | handoff §6.4 | 移除（由日推卡片全面替代）？还是重映射 11 维保留？ |
| 2 | 图鉴弹层未展示示例条目（title + hook） | handoff §5.3 | 补做或降级接受 |
| 3 | 激活上限提示是简版文案，未按 §5.3 内联"当前3个激活中 + 各自放下按钮"列表（需先关弹层手动去放下） | handoff §5.3 | 补做或降级接受 |
| 4 | 新用户首次进入丰荣探索的一次性说明文案未实现 | handoff §5.6 | 补做或降级接受 |
